# Local development — extends requirements.txt with full GPU builds
-r requirements.txt

# Replace CPU-only builds with full builds for local (comment out if on CPU)
# torch  # uncomment to get CUDA build via default index
# tensorflow  # uncomment to get full tensorflow
mtcnn==0.1.1
jinja2==3.1.4
