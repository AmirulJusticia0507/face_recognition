{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/((?!assets|vite\\.svg|favicon).*)",
      "destination": "/index.html"
    }
  ],
  "ignoreCommand": "exit 1"
}
